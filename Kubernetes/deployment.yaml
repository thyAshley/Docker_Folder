apiVersion: apps/v1
kind: Deployment
metadata:
  name: first-app-deployment
spec:
  replicas: 1
  template:
    metadata:
      labels:
        app: first-app
    spec:
      containers:
        - name: kube-node
          image: "thyashley/kube-node"
